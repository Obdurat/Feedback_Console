// import { Navigate, Outlet } from "react-router-dom";
// import { useAuth } from "./AuthProvider";

// export const RequirePasswordChange = () => {
//   const { user } = useAuth();

//   if (user?.firstLogin) return <Navigate to="/first-login" replace />;

//   return <Outlet />;
// };
